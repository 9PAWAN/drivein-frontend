import React from 'react'

const Header =()=>{

    

  return (
    <div>
      <div className='text-primary' style={{backgroundColor:'black'}}>
     <h1>Drive-IN</h1>
     {/* <button
        style={styles.loginButton}
        className="btn btn-primary px-3"
        onClick={() => setLogins(true)}
      >
        {view ? "login" : "logout"}
      </button> */}
      {/* <Login setLogins={setLogins} logins={logins} /> */}
    </div>
    </div>
  )
}

export default Header
